
!define TEST_RUNNER {fitlibrary.suite.FitLibraryServer}

!path ${java.class.path};target/test-classes


!define COMMAND_PATTERN {java -Xmx2000m -cp %p %m}


!define DBUser (${datasourceUsername})
!define DBPassword (${datasourcePassword})
!define DBUrl (${datasourceUrl})
!define VerboseKravtest (${verbose.kravtest})
